import { SupervisoryStaffingNode } from './supervisory-staffing-node';

describe('SupervisoryStaffingNode', () => {
  it('should create an instance', () => {
    expect(new SupervisoryStaffingNode()).toBeTruthy();
  });
});
