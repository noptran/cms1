import { ProviderPreferences } from './provider-preferences';

describe('ProviderPreferences', () => {
  it('should create an instance', () => {
    expect(new ProviderPreferences()).toBeTruthy();
  });
});
